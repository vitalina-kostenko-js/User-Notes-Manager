import {NotesList} from "@/app/widgets/notes";

export default function Home() {
    return (
        <div>
            <main>
                <NotesList/>
            </main>
        </div>
    );
}
